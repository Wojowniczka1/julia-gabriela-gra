def draw():
   if keyPressed: 
        # print(key)
        print(keyCode)
        if keyCode is 39:
           print("dziala")
        if keyCode is 37:
            print("tez dziala")
        
        
